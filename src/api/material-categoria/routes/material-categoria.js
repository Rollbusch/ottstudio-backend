'use strict';

/**
 * material-categoria router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::material-categoria.material-categoria');
