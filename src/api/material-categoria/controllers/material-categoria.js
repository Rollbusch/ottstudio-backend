'use strict';

/**
 * material-categoria controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::material-categoria.material-categoria');
